# Базовий URL опублікованого Web App (Apps Script), без завершального слеша.
# Очікувані шляхи (адаптер lib/sheets-client.ts): GET availability?checkIn=&checkOut= ; POST booking (JSON тіла як у формі).
# SHEETS_WEBAPP_BASE_URL=https://script.google.com/macros/s/XXXX/exec

# Опційно: спільний секрет для заголовка X-Webhook-Secret
# SHEETS_WEBAPP_SECRET=
